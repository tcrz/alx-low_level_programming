#include "main.h"
/**
 * main - prints 'Holberton' followed by a new line
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char x;
	for (x = 95; x <= 122; x++)
	{
		_putchar(x);
	}
	_putchar('\n');
	return (0);
}
